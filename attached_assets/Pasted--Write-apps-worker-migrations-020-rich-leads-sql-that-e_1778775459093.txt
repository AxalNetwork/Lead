
Write apps/worker/migrations/020_rich_leads.sql that extends the lead model. The current leads table has 14 columns and a meta_json blob — promote the most useful fields out of the blob into typed columns. Add these columns to leads:
Identity & contact:
first_name, last_name, middle_name, preferred_name
headline (short bio one-liner)
emails_json (array of {email, type, verified, source, last_seen})
phones_json (array of {phone_e164, type, verified, source, last_seen})
socials_json (array of {platform, handle, url, verified})
personal_site
Persona & demographics:
persona (enum: vc_partner, vc_principal, vc_analyst, angel, lp, founder, gov_official, family_office, corp_vc, scout, advisor, other)
seniority (enum: junior, mid, senior, executive, board)
gender_inferred, gender_source
age_bracket (enum: <30, 30-39, 40-49, 50-59, 60+, unknown) and age_inferred_from (e.g. graduation_year, linkedin, manual)
languages_json
Geography:
country_iso2, region, city
residency_country_iso2
citizenship_iso2_json (array)
timezone
Financial / influence signals (all nullable, all clearly inferential):
net_worth_tier (enum: <1M, 1-10M, 10-50M, 50-250M, 250M-1B, 1B+, unknown)
net_worth_source, net_worth_evidence_url
aum_usd (assets under management — for partners only)
check_size_min_usd, check_size_max_usd
stage_focus_json (array of pre_seed, seed, series_a, …)
sector_focus_json (array of normalized sector tags — see Task 9)
geo_focus_json
Track record:
companies_founded_count, companies_owned_count, board_seats_count
notable_investments_json (array of {company, role, year, outcome})
exits_json, unicorns_json
Workflow:
quality_score (REAL 0-100), confidence_score (REAL 0-100)
priority (enum: p0, p1, p2, p3)
owner_email (which user owns outreach)
do_not_contact (0/1), dnc_reason, dnc_set_at
last_enriched_at, last_verified_at
Indexes: persona, country_iso2, quality_score DESC, last_enriched_at, (persona, country_iso2).
Then create apps/worker/migrations/021_lead_history.sql with a lead_history table:
CREATE TABLE lead_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lead_id INTEGER NOT NULL,
  changed_at TEXT NOT NULL DEFAULT (datetime('now')),
  changed_by TEXT,
  change_kind TEXT NOT NULL,
  field TEXT,
  old_value TEXT,
  new_value TEXT,
  source TEXT,
  evidence_url TEXT
);
CREATE INDEX idx_history_lead ON lead_history(lead_id);
CREATE INDEX idx_history_time ON lead_history(changed_at);
Update apps/worker/src/db/leads.repo.ts (create if missing) to write a history row on every field change. Update apps/worker/src/routes/leads.ts so GET /api/leads/:id returns the full new shape and a new GET /api/leads/:id/history returns the lead_history rows for that lead.
Update the CSV exporter in apps/worker/src/routes/exports.ts to expose the new flattened columns (one row per lead, multi-value fields joined by |).
Do not drop meta_json — keep it as overflow storage for raw scraped fields we haven't promoted yet.
Acceptance criteria:
Migrations apply cleanly (wrangler d1 migrations apply).
GET /api/leads/:id returns the new fields.
Any update via a new internal updateLead() helper writes one lead_history row per changed field.