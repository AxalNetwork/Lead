  taxonomy-mapped leads (not just exact-string matches).
- Creating an ICP and clicking "Match" returns a deterministic list.

---

## Task 10 — Compliance, robots, ToS, PII access log, GDPR/CCPA opt-out

**Replit prompt:**

> Build the compliance layer so axal.vc can defend its dataset.
>
> Migration `070_compliance.sql`:
>
> ```
> CREATE TABLE source_policies (
>   domain TEXT PRIMARY KEY,
>   robots_txt TEXT,
>   robots_fetched_at TEXT,
>   tos_url TEXT,
>   tos_allows_scraping INTEGER,    -- 0/1/-1 (unknown)
>   notes TEXT
> );
> CREATE TABLE dnc_list (
>   id INTEGER PRIMARY KEY AUTOINCREMENT,
>   identifier TEXT NOT NULL,       -- email, phone, linkedin url
>   identifier_kind TEXT NOT NULL,
>   reason TEXT,
>   created_at TEXT DEFAULT (datetime('now')),
>   UNIQUE(identifier_kind, identifier)
> );
> CREATE TABLE pii_access_log (
>   id INTEGER PRIMARY KEY AUTOINCREMENT,
>   user_email TEXT NOT NULL,
>   lead_id INTEGER NOT NULL,
>   fields_json TEXT,
>   reason TEXT,
>   accessed_at TEXT DEFAULT (datetime('now'))
> );
> ```
>
> Middleware: any `/api/leads/:id` GET writes a `pii_access_log` row capturing
> the user_email (from `accessGuard`), lead_id, and which sensitive fields
> (emails, phones, residency, net_worth) were in the response.
>
> Pre-insert hook: before writing any `leads.email` / `leads.phones_json` /
> `leads.socials_json` entry, check `dnc_list`. If match, drop the field and
> set `do_not_contact=1`, `dnc_reason='dnc_list_match'`.
>
> Endpoints:
> - `POST /api/dnc` — add an identifier to the DNC list.
> - `POST /api/leads/:id/dnc` — flag a lead DNC.
> - `GET /api/audit/pii?from=&to=` — export PII access log (admin only).
> - `POST /api/gdpr/erase` — `{email}` → cascades: nulls PII columns on every
>   matching lead, adds to `dnc_list`, writes `lead_history` with
>   `change_kind='gdpr_erasure'`.
>
> robots.txt: `apps/worker/src/scraper/robots.ts` checks `source_policies`
> first, falls back to a fresh fetch when older than 24h. Block fetches
> against disallowed paths.
>
> ToS: a static `apps/worker/data/tos-flags.json` ships with manual entries
> for LinkedIn, Facebook, Instagram, X, Glassdoor (`tos_allows_scraping=0`).
> The pipeline refuses to enqueue direct fetches against those domains and
> emits a clear error.

**Acceptance criteria:**

- Adding an email to `dnc_list` then scraping a page containing it does NOT
  store that email.
- Every `GET /api/leads/:id` writes a `pii_access_log` row.
- `POST /api/gdpr/erase` removes the email from all matching leads in one
  transaction.

---

## Task 11 — Outbound marketing exports + campaign tracking

**Replit prompt:**

> Build the outbound side so marketing can use this dataset without copy-paste.
>
> Migration `080_campaigns.sql`:
>
> ```
> CREATE TABLE campaigns (
>   id INTEGER PRIMARY KEY AUTOINCREMENT,
>   name TEXT NOT NULL,
>   channel TEXT NOT NULL,           -- 'email','linkedin_dm','x_dm','phone'
>   icp_id INTEGER,
>   status TEXT DEFAULT 'draft',
>   created_at TEXT DEFAULT (datetime('now'))
> );
> CREATE TABLE campaign_members (
>   id INTEGER PRIMARY KEY AUTOINCREMENT,
>   campaign_id INTEGER NOT NULL,
>   lead_id INTEGER NOT NULL,
>   status TEXT DEFAULT 'queued',    -- queued,sent,opened,clicked,replied,bounced,unsubscribed
>   sent_at TEXT, opened_at TEXT, clicked_at TEXT, replied_at TEXT,
>   UNIQUE(campaign_id, lead_id)
> );
> ```
>
> Endpoints:
> - `POST /api/campaigns` — create from an ICP id or a saved filter.
> - `POST /api/campaigns/:id/export?format=csv|json|hubspot|lemlist|instantly`
>   — emits the export format the marketing tool expects.
> - `POST /api/campaigns/:id/webhook` — public route (HMAC-signed) that the
>   marketing tool calls back to update `campaign_members.status`.
>
> Export formats:
>
> - `hubspot` — columns matching the Hubspot Contacts CSV importer.
> - `lemlist` — first_name, last_name, company, email, linkedin_url, +
>   liquid-template variables filled from lead fields.
> - `instantly` — Instantly's required schema.
>
> Dashboard: a "Campaigns" tab with new-campaign wizard:
> 1. Pick ICP or filter.
> 2. Pick channel.
> 3. Preview the audience (count + sample table).
> 4. Export.
>
> Add per-lead "campaigns" sub-tab on the lead detail view showing every
> campaign the lead has been in and current status.

**Acceptance criteria:**

- A campaign created from an ICP can be exported as a valid Lemlist CSV that
  imports without errors.
- Posting to the webhook updates `campaign_members.status` and surfaces in
  the funnel analytics from Task 7.

---

## Task 12 — Suggested additional data points & sources to capture

This task isn't a single Replit prompt — it's the list of data points and
sources to integrate progressively into Tasks 3, 4, and 6. Add the
provider where it fits.

**Data points worth adding (beyond Task 3's schema):**

- Education: institutions, degrees, graduation years (drives age inference).
- Employment history with start/end dates (drives seniority and tenure).
- Patents (`patents.google.com`) — deep tech signal.
- Publications / citations (`scholar.google.com`, `orcid.org`).
- Board memberships (public-company DEF 14A filings via SEC EDGAR).
- Government roles (USAspending.gov, EU Transparency Register).
- Political donations (FEC.gov, OpenSecrets) — geopolitical context.
- Real-estate ownership signals (county assessor APIs, ATTOM, where legal).
- Speaking engagements (conference attendee + speaker pages).
- Podcast appearances (Listen Notes API).
- News mentions (GDELT, NewsAPI, MediaCloud).
- Sentiment / influence score (followers, engagement rate from public profiles).
- Co-investor graph (Crunchbase + SEC Form D).
- Time-to-respond statistics (after campaigns run).
- Calendar availability hints (public Calendly / Cal.com pages — opt-in only).

**Source families to integrate, by category:**

- **VC & startup**: Crunchbase, PitchBook (if licensed), CB Insights,
  Tracxn, Dealroom, AngelList, Signal NFX, NFX Marketplace, eu-startups.com.
- **Government & regulatory**: SEC EDGAR, FINRA BrokerCheck, IAPD ADV,
  UK Companies House, OpenCorporates, EU Transparency Register, BODACC (FR),
  SIRENE (FR), Handelsregister (DE), KvK (NL), ASIC (AU), MCA (IN), Hong Kong
  ICRIS, Singapore ACRA, USAspending.gov, GSA SAM.gov.
- **Wealth**: Forbes Billionaires, Forbes Midas List, Bloomberg Billionaire
  Index (public web pages), Hurun Report, family-office databases
  (FINTRX-style — licensed only).
- **Conferences & lists**: Web Summit, SaaStr, Slush, TechCrunch Disrupt,
  Money 20/20, Davos panel pages, YC demo day, fund LP letters.
- **Social public**: X public profiles, public Threads/Bluesky/Mastodon
  profiles, GitHub bios, public Substack about pages.
- **News**: GDELT 2.0 (free firehose), NewsAPI, Diffbot Knowledge Graph,
  MediaCloud.

Capture the source URL and evidence snippet for every field you store, so
quality_score and confidence are auditable later.

---

## Suggested execution order for Replit

1. Task 3 (schema) — cheap, unblocks everything else.
2. Task 1 (scraper engine) — the missing core.
3. Task 5 (anti-bot tiers) — required for Task 1 to actually return data.
4. Task 2 (dashboard scrape UI) — first visible win.
5. Task 8 (dedupe) — needs to be in before bulk runs, else painful cleanup.
6. Task 4 (discovery) — first real lead-flow driver.
7. Task 6 (enrichment) — flips raw URLs into rich leads.
8. Task 9 (taxonomies + ICP) — segmentation.
9. Task 7 (analytics v2) — observability across all of the above.
10. Task 10 (compliance) — must ship before Task 11.
11. Task 11 (campaigns) — outbound activation.
12. Task 12 (incremental sources) — ongoing.

Estimated effort: Tasks 1-8 are ~2-3 weeks of focused Replit work; Tasks 9-11
add ~1-2 weeks. Task 12 is a continuous backlog.

---