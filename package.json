{
  "name": "aidatasignal",
  "version": "0.1.0",
  "private": true,
  "description": "Root package so Cloudflare Workers Builds can install worker dependencies and run `wrangler deploy` from the repo root. The actual worker source lives in apps/worker/.",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "cd apps/worker && npm run typecheck"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^0.0.14",
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.91.0"
  }
}
