- [Production deploy path](prod-deploy-path.md) — workspace HAS CF token (wrangler d1/secret/deploy work; secrets go live instantly) but NO git push auth; canonical deploy = GitHub Action, currently RED (worker fails tsc). Direct `wrangler deploy` ships un-pushed local commits + bypasses gates — avoid.
- [Production behind Cloudflare Access](prod-cloudflare-access.md) — every api.aidatasignal.com endpoint 302s to Access login server-side; can't verify prod endpoints with curl from the workspace.
- [Dashboard asset cache-busting](dashboard-asset-cache.md) — per-page dashboard <script> tags lack ?v= so operators see stale JS after a deploy; "stuck on Loading" is a front-end symptom (stale worker shows empty states, not spinners).
- [Worker test-build quirk](worker-test-build.md) — apps/worker prod build is Bundler, test build (tsconfig.test.json) is NodeNext with an explicit include allow-list; TS2835 extensionless-import errors are expected test-only noise; tsc emits despite errors so verify via node --test on test-dist/.
- [Proxy provider protocols](proxy-provider-protocols.md) — tier2Proxy only speaks GET ?url= forward; ScraperAPI/scrapestack/generic work, Oxylabs/Smartproxy (POST JSON) + Bright Data (Scraping Browser CDP) don't without adapters.
- [Dashboard legacy CSS leak](dashboard-legacy-css-leak.md) — bootstrap/style.css leak white bg onto dashboard panels; under dark theme = white-on-white; fix with scoped `!important` dark bg + light text, not just tokens.
- [Dashboard force-dark surfaces](dashboard-force-dark.md) — to pin a section dark in any theme, re-declare DARK token VALUES on its wrapper, never reference var(--ads-*) (those flip white under [data-theme=light]); verify in LIGHT theme.
- [Investor profile data split](investor-profile-data-split.md) — profile route reads legacy Task-#24 tables (leads/companies/investor_investments) but pipelines write the unified entity store + firm_portfolio/companies_json; investor_investments has no live writer.
- [createEntity null threading](entity-create-null-threading.md) — createEntity & its resolvers return null on the Task #9 garbage-guard path; callers must skip (never fake an entity_id), and any widened-to-null resolver needs every .entity_id deref audited.
- [Firm analytics geo freshness](firm-analytics-geo-freshness.md) — geo endpoint prefers materialized firm_analytics_daily; mutating firm fields needs materializeFirmAnalytics re-run (separate cron branch); use coercers.parseCountryIso2 for country resolution. Also: D1 test shims must compile SQL lazily (defer db.prepare into run/first/all) so insertFact's optional-table .catch probes work.
- [Cross-kind channel dedupe](cross-kind-channel-dedupe.md) — shared findEntityByChannel is NOT kind-filtered; single-kind dedupe (person/firm import) must JOIN u_entities WHERE kind=… or a person can corrupt an org sharing an email/linkedin channel.
- [Legacy file-import resume + watchdog](file-import-resume.md) — file_imports stuck in 'importing' = isolate killed past subrequest budget before catch sets error; chunked cursor resume needs ATOMIC cursor+counter batch, and the watchdog must treat only RECENT import_file jobs as in-flight or orphan queued rows deadlock it forever.
